__pycache__/
*.pyc
build/
dist/
.deps_ok
# NOTE: the two PyInstaller .spec files are tracked on purpose — install.ps1
# builds from them, so a fresh clone cannot install without them.
# downloaded / trained model weights and YOLO run outputs (never source)
*.pt
runs/
.DS_Store
Thumbs.db

# private uploader tool (maintainer only, kept local)
bubblr_publish.py
assets/upload_icon.*
